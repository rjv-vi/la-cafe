{
  "permissions": {
    "allow": [
      "Exec(npm)",
      "Exec(pnpm build)",
      "Exec(pnpm dev)",
      "Exec(node)",
      "Exec(npx)",
      "Exec(ls)",
      "Exec(mkdir)",
      "Exec(cp)",
      "Exec(git add)",
      "Exec(git commit)",
      "Exec(git)",
      "Exec(cat)"
    ]
  }
}